[project]
name = "music-sync"
version = "0.1.0"
description = "Sync liked tracks from streaming services to Bandcamp/Apple Music"
authors = [
    {name = "Alex Yule", email = "adyule@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.32.5",
    "python-dotenv>=1.2.1",
    "browser-cookie3>=0.20.1",
]
